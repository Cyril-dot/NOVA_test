# 4️⃣ Build Docker image
- name: Build Docker image
  run: |
    docker build \
      --file Dockerfile \
      --tag cyrilvibes/nova-backend:latest \
      .

# 5️⃣ Push Docker image to Docker Hub
- name: Push Docker image
  run: docker push cyrilvibes/nova-backend:latest
